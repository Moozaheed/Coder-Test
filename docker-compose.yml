services:
  # PHP Service
  php:
    build: .
    volumes:
      - .:/var/www/html

  # Nginx Service
  nginx:
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - .:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
